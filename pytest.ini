[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    unit: Fast unit tests with no external dependencies (no LLM, no Milvus)
    integration: Tests requiring Milvus or LLM API calls
