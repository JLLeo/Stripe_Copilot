# Stripe AI Sales Agent -- Core Dependencies
# ==========================================

# Web framework and the server the README runs it with
fastapi>=0.135.0
uvicorn>=0.44.0

# Data validation
pydantic>=2.12.0

# Environment
python-dotenv>=1.2.0

# Models (DeepSeek chat + OpenAI embeddings, both via the openai client)
openai>=2.30.0

# Vector database (dense + BM25 sparse collections, Milvus Lite)
pymilvus>=3.0.0
milvus-lite>=3.1.0

# Knowledge graph
networkx>=3.5
PyYAML>=6.0.0

# Seed database build (scripts/build_seed_db.py reads data.xlsx)
openpyxl>=3.1.0

# HTTP client: FastAPI's TestClient (the test and evaluation seam) needs it explicitly
httpx>=0.28.0

# Testing
pytest>=8.0.0
