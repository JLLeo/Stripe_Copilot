# Stripe AI Sales Agent -- Core Dependencies
# ==========================================

# Web framework
fastapi>=0.135.0
uvicorn>=0.44.0
starlette>=1.0.0

# Data validation
pydantic>=2.12.0

# Environment
python-dotenv>=1.2.0

# Models (DeepSeek chat + OpenAI embeddings, both via the openai client)
openai>=2.30.0

# Retained by the not-yet-rewired Milvus ingestion/retrieval modules (removed with the knowledge-search ticket)
langchain-core>=1.2.0
langchain-openai>=1.1.0
langchain-text-splitters>=0.3.0

# Vector database
pymilvus>=3.0.0
milvus-lite>=3.1.0

# Knowledge graph
networkx>=3.5
PyYAML>=6.0.0

# Seed database build (scripts/build_seed_db.py reads data.xlsx)
openpyxl>=3.1.0

# HTTP (transitive)
httpx>=0.28.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
